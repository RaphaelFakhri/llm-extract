target-version = "py311"
line-length = 100
src = ["src", "tests"]

[lint]
select = ["E", "F", "I", "UP", "B"]
ignore = ["B008"]  # typer uses function calls in defaults

[lint.per-file-ignores]
"tests/*" = ["B011"]

[format]
quote-style = "double"
indent-style = "space"
